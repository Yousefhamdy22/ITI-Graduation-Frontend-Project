export interface Instructor {
  id: string;
  name: string;
  email: string;
  bio: string;
  avatar: string;
  expertise: string[];
  coursesCount: number;
  studentsCount: number;
  rating: number;
}
